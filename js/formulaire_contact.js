/*
const btn = document.querySelector('button');
const formulaire = document.querySelector('.contact');
const closed = document.querySelector('.close');

btn.addEventListener('click', () => {formulaire.classList.toggle('is-visible');});

closed.addEventListener('click', () => {formulaire.classList.remove('is-visible');});

*/
